# Google Drive Access Token (Server-side only)
# Get this from Google Cloud Console or OAuth Playground
# The server reads this token from the environment to access Google Drive
# No browser-based OAuth is needed
GOOGLE_DRIVE_TOKEN=your-google-drive-access-token-here

# Cache Configuration
# Set to "true" to use Deno KV for persistent caching (production)
# Set to "false" or omit to use in-memory cache (development/testing)
# USE_DENO_KV=true
